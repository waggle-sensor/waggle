setenv kerneladdr 0x40007FC0
setenv bootargs "console=tty1 console=ttySAC1,115200n8 root=/dev/mmcblk0p2 rootwait rw mem=2047M"


### Boot mmc device 0 partition 1
setenv dev_type "mmc"
setenv dev_num "0"
setenv dev_part "1"

setenv kernelname uImage.1
setenv bootkernel "if test -e $dev_type $dev_num $kernelname; then; ext4load $dev_type $dev_num:$dev_part $kerneladdr $kernelname; if iminfo $kerneladdr; then; echo "Booting $kernelname"; bootm $kerneladdr; fi; fi; "
run bootkernel

setenv kernelname uImage.2
setenv bootkernel "if test -e $dev_type $dev_num $kernelname; then; ext4load $dev_type $dev_num:$dev_part $kerneladdr $kernelname; if iminfo $kerneladdr; then; echo "Booting $kernelname"; bootm $kerneladdr; fi; fi; "
run bootkernel

echo "Both kernels are corrupt"
