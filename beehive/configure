#!/bin/bash
dpkg -i msging/cloud_server/packages_o/*.deb

mkdir /etc/waggle/
rm /etc/smsc95xx_mac_addr
hostName=$RANDOM
echo $hostName > /etc/waggle/hostname

rabbitmqctl add_user guest1 guest1
rabbitmqctl set_permissions guest1 ".*" ".*" ".*"

