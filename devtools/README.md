To build and extend the waggle system, tools are needed, for example
the rduino programming tools and compilers.  This directory contains
some of the basic tools needed to build the software stack for a
Waggle node.
